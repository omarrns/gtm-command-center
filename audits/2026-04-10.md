# GTM Command Center — Architecture Audit

_Run on 2026-04-10 against commit `6b9bf61` — 5 findings_

> Working tree was clean (stash applied before audit). Two untracked files excluded from scope: `.github/workflows/claude-review.yml` and `CHANGELOG.md`.

## Summary

- **P0 (active bugs):** 0
- **P1 (pattern drift):** 0
- **P2 (maintenance):** 2
- **P3 (cosmetic):** 3

No active bugs or pattern drift. Two maintenance items addressed in same session.

---

## ✓ Checks that Passed

- **Check 1 (Supabase client drift):** All 8 `page.tsx` files under `(app)/` use `createSupabaseServiceClient()`. No cookies-client leakage.
- **Check 2 (Loader duplication):** `today-queue.ts` is the single source for `bottom_line` extraction and `groupByDate`. Today, History, and Activate all import from it.
- **Check 3 (Sender-identity compliance):** All 9 prompt builder files accept `SenderIdentity`. Zero hardcoded "Omar" / "Inkeep" strings.
- **Check 4 (Stage transitions):** `advanceStage()` in `opportunities.ts` is the only place that writes `stage`. Zero bypasses.
- **Check 5 (Pipeline step contract):** All steps export `run*` function and `*Result` type. No contract drift.
- **Check 7 (Schema ↔ types):** All 7 allowlisted tables spot-check clean.

---

## P2 — Maintenance

### 1. Silent error swallowing on `normalizeScoringProfile` ✅ Fixed

Four `catch { /* best-effort */ }` blocks with no logging around `normalizeScoringProfile` calls.

| File                                | Line |
| ----------------------------------- | ---- |
| `src/app/(app)/onboard/actions.ts`  | 84   |
| `src/app/(app)/onboard/actions.ts`  | 190  |
| `src/app/(app)/onboard/actions.ts`  | 278  |
| `src/app/(app)/settings/actions.ts` | 131  |

**Fix applied:** Added `console.error("normalizeScoringProfile failed (non-critical):", err)` to each catch. Best-effort semantics preserved; failures now debuggable.

---

### 2. Dead path in CLAUDE.md ✅ Self-resolves with stash

- **File:** `CLAUDE.md:128`
- **Found:** Reference to `src/app/page.tsx` as "was removed" — file is gone.
- **Resolution:** Stashed CLAUDE.md update moves phase notes to `CHANGELOG.md`, eliminating this reference. Resolved on stash apply + commit.

---

## P3 — Cosmetic (deferred)

| File                                                     | Lines | Extraction candidate                                                   |
| -------------------------------------------------------- | ----- | ---------------------------------------------------------------------- |
| `src/app/(app)/onboard/_components/onboard-client.tsx`   | 757   | `StepAboutYou`, `StepSearchPrefs`, `StepOutreach` sub-components       |
| `src/app/(app)/onboard/_components/review-client.tsx`    | 600   | `ProfileReviewSection`, `SearchReviewSection`, `OutreachReviewSection` |
| `src/app/(app)/settings/_components/settings-client.tsx` | 565   | `PipelineConfigSection`, `ScoringWeightsPanel`, `GmailConnectionPanel` |

No urgency. Address when already editing these files.
