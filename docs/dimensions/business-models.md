# Business models (`firmographics.business_model`)

**Canonical enum and copy:** [`business-models.json`](business-models.json) — nine `values` with `tagline`, `definition`, `examples`, `primary_metrics`, and `takeaways` for prompts, review UI, and `icp-dimensions.ts`.

- **This dimension** = how the **target company** makes money (revenue / commercial motion).
- **Not** [`product.delivery_model`](delivery-model.json) (how _your_ product is delivered).
- **Architecture:** [`icp-rubric-architecture.md`](../icp-rubric-architecture.md).

To change labels, metrics, or examples, edit **`business-models.json`**; keep this file as a short pointer for readers browsing the repo.
